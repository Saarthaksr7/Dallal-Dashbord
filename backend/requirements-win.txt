# Dallal Dashboard - Windows Requirements
# Platform: Windows
# Python: 3.8+

# Core Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
gunicorn>=21.2.0  # Production WSGI server (works on Windows with limitations)

# Rate Limiting & Security
slowapi>=0.1.9
python-jose[cryptography]>=3.3.0
pyjwt>=2.8.0
passlib[bcrypt]>=1.7.4
argon2-cffi>=23.1.0

# Database & ORM
sqlmodel>=0.0.14
pydantic-settings>=2.0.3

# File Upload & Processing
python-multipart>=0.0.6

# HTTP Client
httpx>=0.25.0
requests>=2.31.0

# SNMP Support
pysnmp>=4.4.12

# Docker SDK
docker>=6.1.3

# Task Scheduling
apscheduler>=3.10.4

# Network Discovery
zeroconf>=0.120.0

# Logging
loguru>=0.7.2

# Network Utilities
mac-vendor-lookup>=0.1.12
wakeonlan>=3.0.0

# System Monitoring
psutil>=5.9.6

# SSH/SFTP Support (Windows compatible)
paramiko>=3.3.1

# Optional but recommended for production
redis>=5.0.0  # Caching
psycopg2-binary>=2.9.9  # PostgreSQL adapter
pymysql>=1.1.0  # MySQL adapter (alternative)
sentry-sdk[fastapi]>=1.38.0  # Error tracking
prometheus-fastapi-instrumentator>=6.1.0  # Metrics

# Windows-specific
pywin32>=306; sys_platform == 'win32'  # Windows API access
colorama>=0.4.6; sys_platform == 'win32'  # Colored terminal output on Windows

# Email support (if needed)
aiosmtplib>=3.0.0
email-validator>=2.1.0
